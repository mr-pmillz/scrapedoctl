name: Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write
  packages: write

env:
  UPX_VERSION: "5.1.1"
  PWSH_VERSION: "7.6.0"
  GORELEASER_VERSION: "~> v2"

jobs:
  goreleaser:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # ratchet:actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Set up Go
        uses: actions/setup-go@4a3601121dd01d1626a1e23e37211e3254c1c06c # ratchet:actions/setup-go@v6
        with:
          go-version: '1.26'
          cache: true

      - name: Install UPX from GitHub
        run: |
          curl -fsSL "https://github.com/upx/upx/releases/download/v${UPX_VERSION}/upx-${UPX_VERSION}-amd64_linux.tar.xz" -o upx.tar.xz
          tar xJf upx.tar.xz
          sudo mv "upx-${UPX_VERSION}-amd64_linux/upx" /usr/local/bin/
          rm -rf upx.tar.xz "upx-${UPX_VERSION}-amd64_linux"
          upx --version | head -1

      - name: Install PowerShell from GitHub
        run: |
          curl -fsSL "https://github.com/PowerShell/PowerShell/releases/download/v${PWSH_VERSION}/powershell-${PWSH_VERSION}-linux-x64.tar.gz" -o pwsh.tar.gz
          sudo mkdir -p /opt/microsoft/powershell/7
          sudo tar xzf pwsh.tar.gz -C /opt/microsoft/powershell/7
          sudo chmod +x /opt/microsoft/powershell/7/pwsh
          sudo ln -sf /opt/microsoft/powershell/7/pwsh /usr/local/bin/pwsh
          rm pwsh.tar.gz
          pwsh --version

      - name: Generate completions
        run: |
          mkdir -p completions
          go run ./cmd/scrapedoctl completion bash > completions/scrapedoctl.bash
          go run ./cmd/scrapedoctl completion zsh > completions/scrapedoctl.zsh
          go run ./cmd/scrapedoctl completion fish > completions/scrapedoctl.fish
          go run ./cmd/scrapedoctl completion powershell > completions/scrapedoctl.psm1
          VERSION="${GITHUB_REF_NAME#v}"
          pwsh -Command "New-ModuleManifest -Path ./completions/scrapedoctl.psd1 -RootModule './scrapedoctl.psm1' -Author 'Pavel Lavrukhin' -CompanyName 'mr-pmillz' -Description 'PowerShell completion for scrapedoctl' -ModuleVersion '${VERSION}' -FunctionsToExport @() -CmdletsToExport @() -VariablesToExport @() -AliasesToExport @()"
          ls -la completions/

      - name: Run GoReleaser
        uses: goreleaser/goreleaser-action@5daf1e915a5f0af01ddbcd89a43b8061ff4f1a89 # ratchet:goreleaser/goreleaser-action@v7
        with:
          distribution: goreleaser
          version: ${{ env.GORELEASER_VERSION }}
          args: release --clean
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
